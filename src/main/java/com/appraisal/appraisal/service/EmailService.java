//package com.appraisal.appraisal.service;
//
//public interface EmailService {
//
//    /**
//     * Sends a plain-text email. Implementations must never throw -
//     * failures are logged so a broken/unconfigured mail server can
//     * never break the underlying business action (goal approval, etc.).
//     */
//    void sendEmail(String to, String subject, String body);
//}
